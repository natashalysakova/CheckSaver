@model IEnumerable<CheckSaverCore.tmp.ExtentionsModels.Stores>

@{
    ViewBag.Title = "Магазины";
}

<div class="container">

    @foreach (var item in Model)
    {
        <div class="col-md-4 col-sm-6 col-xs-12 col-lg-3 nopaddiing">
            @{ Html.RenderPartial("Store", item); }
        </div>

                }
</div>

<div class="round-button">
    <div class="round-button-circle" onclick="location.href = '@Url.Action("Create", "Stores")'; ">
        +
    </div>
</div>

@*<div id="delete">
    @{
        Html.RenderPartial("Delete", new CheckSaver.Models.Stores() {Address = "fdfs", Title = "fdsfdsfsfadad" });
    }

</div>*@