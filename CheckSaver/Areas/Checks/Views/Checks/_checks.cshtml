@model IEnumerable<CheckSaver.Models.Checks>
@using CheckSaver.Helpers;

<div class="row">
    @Ajax.PageNavigator((int)ViewData["PageNum"], (int)ViewData["PageCount"])
</div>

@foreach (var item in Model)
{
    <div class="col-md-4 col-sm-6 col-xs-12 col-lg-3 nopaddiing">
        @{ Html.RenderPartial("Card", item); }
    </div>

}

