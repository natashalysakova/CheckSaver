@model IEnumerable<Invoice>

@{
    ViewBag.Title = "Квитанции";
    Layout = "~/Views/Shared/_InvoiceLayour.cshtml";

}


<div class="container">
    @foreach (var item in Model)
    {
        Html.RenderPartial("_invoice", item);
    }
</div>

<div class="round-button">
    <div class="round-button-circle" onclick="location.href = '@Url.Action("Create", "Invoices")'; ">
        +
    </div>
</div>